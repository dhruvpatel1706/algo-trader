"""Reinforcement-learning moonshot lane.

Paper-only research scaffold. Submodules MUST keep `LIVE_BROKER_BRIDGE = False`;
the test in `tests/unit/moonshot/test_bridge_invariant.py` enforces that
recursively across the whole moonshot package.
"""

LIVE_BROKER_BRIDGE: bool = False
